version: '3.8'

services:
  vocalog-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vocalog-ai-pipeline
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
